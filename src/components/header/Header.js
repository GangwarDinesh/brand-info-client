import React from 'react';
import styles from './header.module.css';

const header = (props) => {
    return (
        <div className={styles.Header}>
                <h3>Products Brand informer</h3>
        </div>
    );
} 

export default header;